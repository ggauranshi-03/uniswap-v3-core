{
  "name": "usar_dex",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ usar_dex",
    "create-local": "graph create --node http://localhost:8020/ usar_dex",
    "remove-local": "graph remove --node http://localhost:8020/ usar_dex",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 usar_dex",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.87.0",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
